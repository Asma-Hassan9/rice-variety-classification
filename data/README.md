# Data

Download the [Rice Image Dataset](https://www.kaggle.com/datasets/muratkokludataset/rice-image-dataset) directly from Kaggle.

The notebook uses `kagglehub.dataset_download()` and discovers the downloaded `Rice_Image_Dataset` directory automatically. Dataset images are excluded from version control.

Never commit `kaggle.json`, API keys, access tokens, or other credentials.

